export interface Event {
    id: number;
    name: string;
    description: string;
    tags: number[];
    startDate: Date;
    endDate: Date;
    image: string
    location: number;
    color: string;
}
